apiVersion: v1
kind: Service
metadata:
  name: cs-fundamentals
  labels:
    app: cs-fundamentals
spec:
  selector:
    app: cs-fundamentals
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  type: ClusterIP
